# Deputy Fleet-State Snapshot — Arc Alignment — 2026-05-28T21:47:05-0700

Scope: arc-alignment + collision-detection for the registered substrate-resolver fleet, per Helmsman CAACP request `d1c7422f-9550-4117-9af5-f54432901194`.

## 1. Active Substrate-Resolver Instances

Sidecar health at snapshot time showed the registry still contains old/null or duplicate substrate-resolver registrations. Live status below is based on running bridge processes, not token registry alone.

| instance_id | bridge thread | bridge PID | log path | last_seen_message_id | current locale |
|---|---|---:|---|---|---|
| `codex-pop-os-20260529t040621` | `019e7112-011a-7723-9a58-81f96c091352` (`Resolver 2`) | `222753` | `apparatus/caacp-server/data/bridge-substrate-resolver-codex-pop-os-20260529t040621-codex-app.log` | `bad912dd-d314-4dac-a25c-5c44dc251dc5` | idle/unknown. No resolver-specific artifact links this instance to a locale after re-init. |
| `codex-pop-os-20260529t040708` | `019e7112-8835-7331-9058-06fcefce110f` (`Resolver 3`) | `237089` | `apparatus/caacp-server/data/bridge-substrate-resolver-codex-pop-os-20260529t040708-codex-app.log` | `bad912dd-d314-4dac-a25c-5c44dc251dc5` | idle/unknown. No resolver-specific artifact links this instance to a locale after re-init. |
| `codex-pop-os-20260529t043820` | `019e710f-40f8-7953-a9c4-e049755e8958` (`Resolver 1`) | `237410` | `apparatus/caacp-server/data/bridge-substrate-resolver-codex-pop-os-20260529t043820-codex-app.log` | none in bridge seen-cache yet | idle/unknown. Bridge started after the re-init wave; no work message seen in its bridge log. |

Probably inactive or stale substrate-resolver registrations still in sidecar health:

- `codex-pop-os-20260529t040618`: registered, inbound file exists, no live bridge process found.
- Duplicate `codex-pop-os-20260529t040621`: sidecar health shows two registrations for the same instance id. Treat the bridge process above as the live one.
- Duplicate `codex-pop-os-20260529t040708`: sidecar health shows two registrations for the same instance id. Treat PID `237089` as the live one.
- `codex-substrate-resolver-4`: registered, inbound file exists, no live bridge process found.

Singleton-tier stale/null registrations still present and probably inactive for routing: `watcher` instance `null`, `arbiter` instance `null`, `deputy` instance `null`. Instance-scoped live roles are `watcher-pop-os-20260529t043910`, `codex-pop-os-20260529t043738`, and `codex-pop-os-20260529t043807`.

## 2. Active Arcs With Parallelization Candidates

### `2026-05-25-ecmascript-parity-shared-upstream`

Telos: close five shared-upstream constraints and test whether the projected sample-rate cascade materializes.

Roster:

| sub-locale | status | likely source dependency |
|---|---|---|
| `host-262-shim` | queued/open | host/harness surface, likely low collision with parser/runtime arcs |
| `iterator-close-on-abrupt` | queued/open | bytecode + runtime abrupt-completion paths; collides with iterator arc and AGFA |
| `parser-permissiveness-audit-extensions` | queued/open | parser, collides with strict-mode/parser early-error work |
| `strict-mode-parser-tracking` | queued/open | parser state, collides with parser-permissiveness if run simultaneously |
| `host-method-prologue-discipline` | queued/open | runtime intrinsic/host-method registration, likely broad but separable from parser work |

Parallelization grade: **HIGH**, if Helmsman chooses non-overlapping lanes. Best three-way split: host shim, one parser lane only, host-method/runtime lane. Do not run both parser sub-locales in parallel.

### `2026-05-28-array-exotic-substrate`

Telos: Array/TypedArray indexed-exotic length/species/data-property paths.

Roster status: most sub-locales landed. `typed-array-wrong-result` reached Phase-5 inflection at TAWR-EXT 6 and routes residuals to deferrals. `typed-array-resizable-buffer-indexed-access` is green through TARBIA-EXT 2 for its RAB-indexed slice.

Open versus landed:

- OPEN by arc file: `typed-array-wrong-result`, `typed-array-resizable-buffer-indexed-access`.
- Practically closed/deferral-routed by trajectory tail: TAWR closed the productive arc at EXT 5, EXT 6 was negative/reverted and emitted `ta-element-coercion-spec-faithful`; TARBIA current slice is green.

Source dependencies: runtime `interp.rs` + `intrinsics.rs` + typed-array backing-store helpers. This is a hot locus and should not be split across agents without founding new deferral locales first.

Parallelization grade: **LOW**. Treat as chapter-close/deferral routing, not a 3-agent work surface.

### `2026-05-28-iterator-protocol-substrate`

Telos: IteratorRecord-respecting bytecode emission and IteratorClose on every consuming site.

Roster:

| sub-locale | status | likely source dependency |
|---|---|---|
| `iterator-close-emission-sites` | LANDED | compiler/runtime |
| `iterator-close-on-abrupt` | LANDED | compiler/runtime |
| `iterator-protocol-error-propagation` | LANDED | runtime protocol |
| `iter-protocol-bytecode-rewrite` | LANDED | bytecode rewrite |
| `for-of-destructuring-assignment-semantics` | LANDED | parser/compiler |
| `for-in-prototype-chain` | LANDED | runtime enumeration |
| `generator-coroutine-suspension` | OPEN | runtime execution model |
| `async-generator-and-for-await-lowering` | OPEN but residual-cautioned | compiler/runtime/intrinsics |
| `iterable-primitive-tobject` | LANDED | runtime conversion |

AGFA tail says remaining rows are async-generator suspension / `yield*` protocol and should be left unless entering with a timeout-safe design.

Source dependencies: `compiler.rs`, `interp.rs`, `intrinsics.rs`, generator/async runtime. Heavy overlap with lowering and eval-context work.

Parallelization grade: **LOW-MEDIUM**. At most two lanes if one is pure analysis/baseline and one is implementation. Not a clean 3-agent split.

### `2026-05-28-json-serialization-carve-out`

Telos: partition and close remaining `JSON.stringify` sub-coordinates.

Roster: one active locale, `json-stringify-semantics`.

Status: JSS-EXT 5 closed replacer-function holder/root-wrapper semantics. Remaining carve-outs: gap/space, BigInt/toJSON ordering, wrapper substitution, lone-surrogate escaping, and proxy/realm availability.

Source dependencies: mostly runtime JSON helpers and string/BigInt/proxy availability. Single active locale means split would require founding child locales first.

Parallelization grade: **LOW**. Sequential unless Helmsman explicitly decomposes into child locales.

### `2026-05-28-lex-tier-conformance`

Telos: ECMA §11-§12 token stream with preserved bit flags.

Roster status: all listed sub-locales are LANDED in `arc.md`.

Source dependencies: lexer/parser boundary only if a new residual is surfaced.

Parallelization grade: **LOW**. No obvious open implementation lane; use as upstream dependency context for parser arc.

### `2026-05-28-lowering-feature-gap-triage`

Telos: partition and close bytecode compiler "not yet supported" residues.

Roster:

| sub-locale | status | likely source dependency |
|---|---|---|
| `cruft-lowering-feature-gaps/` | parent founded; tail clusters largely exhausted | `compiler.rs` |
| `cruft-lowering-feature-gaps/super-reference-lowering/` | SRL-EXT 6 closed 17/22; five runtime residuals split into three mechanisms | `compiler.rs`, `interp.rs`, `module.rs`, `value.rs` |

Tail state: CLFG-EXT 5 says parent telos is substantially achieved, with the remaining super-direct-eval runtime cluster deferred by policy. SRL residuals split into object-literal HomeObject eval, class-field/arrow `super` runtime receiver/private semantics, and derived-constructor direct-eval `super()` this-cell rebinding.

Source dependencies: very hot. Recent commits repeatedly touched `pilots/rusty-js-bytecode/derived/src/compiler.rs` and `pilots/rusty-js-runtime/derived/src/interp.rs`.

Parallelization grade: **MEDIUM** only after founding three child locales for the three SRL residual mechanisms. Without that split, it is collision-prone and should be sequenced.

### `2026-05-28-parser-early-error-conformance`

Telos: parser-tier AST + early-error verdicts at spec production boundaries.

Roster status: all listed parser sub-locales are LANDED, with `finally-abrupt-completion-lowering` provisionally enrolled despite compiler-lowering shape.

Source dependencies: parser `stmt.rs`/`expr.rs`, with occasional compiler spillover.

Parallelization grade: **LOW** until a fresh PCR failure table or new child coordinate is named.

### `2026-05-28-typescript-resolver-arc`

Telos: `.ts` source bytes to executable JS erasure preserving TS runtime contracts.

Roster status: parent TSR and listed sub-locales are LANDED; TCC/TXC apparatus pilots are the measurement siblings.

Source dependencies: TypeScript resolver + module-loader extension only if TCC/TXC surfaces fresh failures.

Parallelization grade: **LOW** until TCC/TXC emits a new failure table and Helmsman founds child work.

## 3. Anticipated Coordination Concerns

Open proposals: none under `apparatus/proposals/pending/`.

Potential turf overlap:

- No live substrate-resolver currently has a locale assignment visible in CAACP bridge logs or repo artifacts after re-init. Assignments should be explicit in CAACP before any substrate work starts.
- `compiler.rs`, `interp.rs`, and `intrinsics.rs` are the hot loci. In the last 12 hours they appeared in 11, 11, and 10 recent commit file lists respectively. Any two agents assigned to lowering, iterator, eval, or async-generator work will likely collide unless their files are partitioned.
- `AGENTS.md`, `CLAUDE.md`, `agent-init-protocol.md`, and role skills are also hot, but those are governance/service apparatus surfaces, not substrate targets.

Recent hot-locus source files:

| file | recent commit-file appearances | concern |
|---|---:|---|
| `pilots/rusty-js-runtime/derived/src/interp.rs` | 11 | runtime execution, eval, iterator, async, typed-array overlap |
| `pilots/rusty-js-bytecode/derived/src/compiler.rs` | 11 | lowering, eval, iterator, parser-to-bytecode overlap |
| `pilots/rusty-js-runtime/derived/src/intrinsics.rs` | 10 | host/intrinsic registration, async/iterator, JSON/typed-array overlap |
| `pilots/rusty-js-runtime/derived/src/module.rs` | 4 | eval/module boundary overlap |

Stale/null registrations:

- Singleton null registrations remain for watcher, arbiter, deputy. Use instance-scoped registrations for live routing.
- Substrate-resolver stale registrations likely include `codex-pop-os-20260529t040618` and `codex-substrate-resolver-4`.
- Duplicate substrate registrations exist for `codex-pop-os-20260529t040621` and `codex-pop-os-20260529t040708`; treat the live bridge PID as authoritative.

Working tree note: `git status --short` showed only the Deputy's prior untracked fleet-state file before this snapshot. No substrate dirty files were present.

## 4. Recommended Next Move For Helmsman

Top arcs by parallelization grade:

1. **`2026-05-25-ecmascript-parity-shared-upstream`** — best 3-agent candidate because it has independent queued sub-locales across host/harness, parser, and runtime-registration surfaces.
2. **`2026-05-28-lowering-feature-gap-triage`** — second-best only if Helmsman first founds three child locales for the SRL residual split. Otherwise it is too hot-locus/collision-heavy for parallel implementation.

Recommended assignment if Helmsman chooses EPSUA:

| resolver instance | assignment | caveat |
|---|---|---|
| `codex-pop-os-20260529t040621` | `host-262-shim` | Low collision, additive host/harness work. Good first lane. |
| `codex-pop-os-20260529t040708` | `strict-mode-parser-tracking` OR `parser-permissiveness-audit-extensions` | Pick exactly one parser lane, not both. Parser lanes should not run simultaneously. |
| `codex-pop-os-20260529t043820` | `host-method-prologue-discipline` | Runtime/intrinsic registration lane; keep away from iterator/async work until finished. |

Do-not-touch-yet notes:

- Do not assign two resolvers to `compiler.rs` work simultaneously (`lowering-feature-gap-triage`, iterator abrupt close, async-generator, direct-eval super all converge there).
- Do not continue AGFA implementation unless Helmsman explicitly accepts the async-generator suspension design risk; the trajectory tail warns against widening bridges blindly.
- Do not treat Array Exotic as a broad active work pool. Its productive arc appears Phase-5 closed; residuals should found new locales (`ta-element-coercion-spec-faithful`, RAB follow-ons) before parallel work starts.
