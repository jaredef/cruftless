# ast-bytecode-uncategorized-projection — Diff-Prod Analysis

No diff-prod fixtures directly exercise this locale's scope. This locale is an apparatus-hardening workstream (classification: APPARATUS GAP), not an engine-fix locale. Its telos is to refine the test262 categorizer to extract sub-projections from uncategorized `reason` text in the 659-fixture `uncategorized/projection` bucket. Once the categorizer improves, these fixtures will redistribute into other engine-DAG coordinates for substrate work. Diff-prod results are not informative for apparatus instrumentation work.
