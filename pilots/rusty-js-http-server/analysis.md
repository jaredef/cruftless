# rusty-js-http-server — Diff-Prod Analysis

No diff-prod fixtures directly exercise this locale's scope. This locale targets host/infrastructure APIs outside the ECMA-262 behavioral surface. HTTP server capability, Node http.createServer wireup, and Compartment realm dispatch are infrastructure concerns measured by the locale's own Express probe and loopback-HTTP fixture, not by diff-prod.
