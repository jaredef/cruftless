# ts-resolve — Diff-Prod Analysis

No diff-prod fixtures directly exercise this locale's scope. TS source lowering is measured by TCC/TXC apparatus. The ts-resolve locale handles TypeScript type-erasure as a source-language resolver upstream of rusty-js-ir. Parse-parity is at 100.0% (374/374) on the real npm corpus; execute-parity at 70.9%. Diff-prod fixtures are all pure JavaScript.
