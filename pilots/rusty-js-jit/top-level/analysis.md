# rusty-js-jit/top-level — Diff-Prod Analysis

No diff-prod fixtures directly exercise this locale's scope. Diff-prod measures correctness; this locale targets performance measured by CRB. Top-level JIT extends entry mechanism to module bytecode and broadens the JIT alphabet for json_parse_transform's inner loop; yield measured by CRB wall-clock on that fixture.
