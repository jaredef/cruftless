# ts-resolve-enums — Diff-Prod Analysis

No diff-prod fixtures directly exercise this locale's scope. TS source lowering is measured by TCC/TXC apparatus. This locale handles TypeScript enum declarations in TSR's strip pipeline (MVP: strip enum entirely; full lowering deferred). Yield measured by TXC execute-parity (52.7% to 58.6%, +5.9 pp).
