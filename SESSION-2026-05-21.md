# Session 2026-05-21 — Path toward parity

## Snapshot

| Metric | Open | Close |
|---|---|---|
| Parity | 94.9% (113/119) | **95.7% (114/119)** |
| Commits this session | — | 7 |
| Substrate landings | — | 3 |
| Reading rungs (no-commit) | — | 2 (enquirer deferred, Callable verified-non-gap) |
| Deviation pipelines built | 0 | 1 (arktype, Doc 730 §XII–§XVII) |
| Locales founded | — | 1 (rusty-js-esm) |

## The path to ≥98% (117/119)

Five residual failures. Each has a documented disposition.

### In-locale (ESM namespace synthesis), one resolved-but-incomplete

1. **arktype** — substrate gap #1 (`class X extends Array` subclass identity) **resolved** in §XVI (commit 764d7f88). arktype still FAIL because of a downstream gap surfaced by the closure; §XVII iter 2 queued. Closing it requires further upstream instrumentation (bindReference / parseDefinition / per-kind intersection impls). The bracket-Callable probe ruled out the obvious next substrate candidate.

### Out-of-locale, with clear forwarding

2. **node-fetch** — bun has a built-in node-fetch compatibility shim. Re-categorized to `fetch-api` / `node-http` compatibility-shim layer (Rung-2 trajectory entry). Not a cruftless substrate gap.

3. **superagent** — original `buf.values` crash **unblocked** by the TypedArray/Buffer iterator triplet (commit b2927524); now fails at a distinct downstream `require('../..')` resolution gap inside `superagent/lib/node/agent.js`. Forwards to module-resolution work.

4. **entities** — TIMEOUT (bun_rc=0 rb_rc=124). Either a hang in evaluation or a slow path. Not yet investigated; queued as its own deviation pipeline run.

5. **enquirer** — bun keeps 18 of 41 `enumerable: false` own properties (EventEmitter API + function intrinsics) and drops the other 23 (PascalCase class refs + 3 utility names). Bun's filter rule is package-shape-dependent in a way the locale's reading cannot recover without bun source access. Deferred (Rung-3 trajectory entry); needs a smaller-shape reading.

## What the deviation-resolution pipeline gives us going forward

The arktype run produced a reusable framework: `pipelines/rusty-js-esm/deviations/<pkg>/` with `pipeline.md`, `trajectory.md`, `run-pipeline.sh`, `captures/`, `traces/`, `probes/`. Each future deviation gets the same shape:

- §XII Capture → L0 result JSON.
- §XIII Reduce → progressive input narrowing.
- §XIV Localize → engine-pair tracing with source-side probes.
- §XV Bracket → package-independent probe at the substrate primitive.
- §XVI Yield → substrate move iff bracket flips AND sweep clean.
- §XVII Iterate → re-enter the pipeline on the next divergent point.

The discipline says: **bracket-PASS = not the gap, stop chasing**. That's how the Callable iteration closed cleanly today instead of degenerating into arktype source-reading.

Apply this to entities and the arktype §XVII follow-up next. enquirer's resistance to outside reading argues for a different approach (instrumentation of cruftless itself rather than the package) — also queued.

## Anti-telos held

The locale's anti-telos §I.2:
- *No blanket synthesis*: gates required on every substrate move. ✓ (4-condition gate on Rung-5; 32-hop proto-chain walk on Rung-6.)
- *No probe-skipping at sweep tier*: every move ran the full 119-package sweep before commit. ✓
- *No net-negative rounds*: Rung-5 caught the lodash-es regression at sweep tier and was tightened before commit. ✓

## Net forward

The high-leverage path to 98%+ now runs through the deviation-resolution pipeline applied to each remaining failure, not through speculative substrate moves. The pipeline produces artifacts that compose across sessions; the next session resumes from the trajectory files without re-walking.
