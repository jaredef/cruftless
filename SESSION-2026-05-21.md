# Session 2026-05-21 — Path toward parity (close-out 2)

## Snapshot

| Metric | Open | Close (rev 1) | **Close (rev 2)** |
|---|---|---|---|
| Parity | 94.9% (113/119) | 95.7% (114/119) | **99.1% (118/119)** |
| Commits this session | — | 7 | **15** |
| Substrate landings | — | 3 | **6** |
| Compatibility shims | — | 0 | **1** (node-fetch) |
| Reading rungs | — | 2 | **5** |
| Deviation pipelines built | 0 | 1 | **1, iterated twice** |
| Corpus docs published | — | 0 | **1** (Doc 730 §XVIII) |

## Substrate landings (six)

1. **Rung-5** (`module.rs`) — dual-package default synthesis. Gates: namespace.default undefined AND package.json has both `main` and `module` AND no `exports` field AND `main != module`. Probe: superstruct.
2. **TypedArray + Buffer iterator triplet** (`intrinsics.rs`, `node_stubs.rs`) — values/keys/entries on ta_proto and Buffer instances, with self-iterable iterator objects. Unblocks `for...of buf.values()`.
3. **Op::New Array-subclass identity** (`interp.rs`) — when `callee.prototype`'s proto chain reaches `Array.prototype`, pre-allocate the `this` as `Object::new_array()` so Array's intrinsic mutates the receiver in place. Bracket: `class Sub extends Array; new Sub(...).constructor === Sub`. Spec: §22.1.2.1 + §10.1.13.
4. **CJS directory main-field resolution** (`module.rs` `probe_with_extensions`) — when candidate is a directory, read package.json's `main` field BEFORE falling back to index.*. Closes `require('../..')` in superagent.
5. **`http.METHODS` adds QUERY** (`cruftless/src/http.rs`) — RFC 9110 method present in bun, absent in cruftless; superagent loop missed one method without it.
6. **`String.prototype.indexOf` honors fromIndex** (`interp.rs`) — implementation previously coerced position then discarded it. Per §22.1.3.9 step 5, clamp to [0, char_count] and search the suffix. Closes the entities infinite loop.
7. **Rung-7 superclass-prototype enum filter** (`module.rs` `populate_cjs_namespace_view`) — when exports is function-typed with a non-trivial superclass prototype: keep enum=true always; for enum=false keep only `{name,length,prototype}` ∪ `superProto.ownNames`. Drops user-defined `enum=false` PascalCase statics. Closes enquirer.
8. **`ArraySpeciesCreate` honors `O.constructor`** (`interp.rs`) — §22.1.3.17 step 3-6. Per the §XVI.iter2 pipeline pass on arktype. Closes the original arktype `rawIn-on-Array` signature.

(That's eight bullets because some commits cover two paired changes: the EventEmitter prototype additions for Rung-7, the http.METHODS+directory-resolution paired commit, etc. The session shipped six discrete substrate-tier improvements + one compatibility shim.)

## Compatibility shim (one)

- **node-fetch** (`cruftless/src/module_ns.rs`) — bun ships a built-in node-fetch interceptor; cruftless mirrors its two extra namespace keys (`fetch` aliased to default, synthesized `FetchBaseError` extending Error). Gated on (1) URL contains `/node_modules/node-fetch/` AND (2) package.json `name` equals `node-fetch`. Strictly additive.

## Reading rungs (five)

- **Rung-2** (default-function named-alias) — REJECTED via counter-probe; bun doesn't do general ESM named-aliasing.
- **Rung-3** (enquirer CJS-namespace filter) — initially deferred as "unrecoverable"; later REVERSED via the §XVIII recovery axis (structural set-membership). Landed as Rung-7.
- **Rung-4** (superstruct re-export) — design closed; landed as Rung-5.
- **arktype §XVII iter 1** (Callable bracket) — PASS in both engines; pattern is not the gap.
- **arktype §XVII iter 3** (class-field + explicit-return-from-super) — PASS in both engines; the obvious bracket disproves this hypothesis. Deeper instrumentation queued.

## Pipeline framework + corpus

- **Deviation-resolution pipeline** built and executed end-to-end on arktype: `pipelines/rusty-js-esm/deviations/arktype/` with `pipeline.md`, `trajectory.md`, `run-pipeline.sh`, `captures/L{0..11}`, `traces/L{4..11}-{bun,cruftless}.jsonl`, four bracket probes. Iterated twice (§XVI + §XVI.iter2).
- **Doc 730 §XVIII** published (corpus-master `3638e8b` + resolve mirror `5d5b923` pushed + seed clean). The appendix formalizes the §XVIII recovery protocol (structural set-membership as a second reading axis when descriptor-only reading admits no rule).

## Path to 100%

One residual: **arktype**. The §XVII iter 3 gap is the lone holdout. Two substrate gaps deeper than session start; the next move is the §XVIII structural-set-membership reading against the intermediate constructors invoked between `parseGeneric` and the failing `params` getter. This is genuine focused-session work, not a quick rung.

If §XVII iter 3 lands cleanly, arktype flips to PASS and parity is 100% (119/119). If it surfaces another downstream gap (the pipeline expects this — each iter unblocks the next), the cycle continues until either a substrate move lands the package or all bracket probes return PASS and the deviation is declared package-internal logic (out of cruftless scope).

## What the locale's discipline produced

Per the anti-telos §I.2:
- Every substrate move ran the full 119-package sweep before commit.
- Two near-regressions caught at sweep tier and gates tightened in-place (Rung-5 lodash-es; the implicit close on the Callable bracket).
- One initially-deferred rung reversed via the §XVIII protocol (enquirer Rung-3 → Rung-7), formalized as a corpus refinement.

The pipeline produces artifacts that compose across sessions. The next session resumes from `pilots/rusty-js-esm/deviations/arktype/trajectory.md` without re-walking; all bracket probes, L0-L11 traces, and instrumented-source patches are re-runnable from the run-pipeline.sh script.

## Cumulative engagement standing

Tier-Ω cutover audit's decision gates:
- ≥96% (Round-3 completion gate): **cleared** (entered the session above this).
- ≥98% (locale telos): **cleared** (98.3% at entities landing; 99.1% after enquirer).
- 99%+: **cleared**.
- 100%: gated on arktype only.

The hand-rolled engine is now the parity-completion target. `legacy/host-rquickjs` remains available as opt-in via `RB_BIN=` override but is no longer the load-bearing measurement. The engagement has transitioned from "build the engine" to "close the last 0.8%."
